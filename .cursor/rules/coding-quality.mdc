---
description: 编码质量 — DRY 抽取边界与性能场景
alwaysApply: true
---

# Coding Quality

## DRY（重复代码）

- **出现 2+ 次、语义相同** 的逻辑（解析、IPC 封装、历史写入、校验）→ 抽到共享函数/模块。
- **仅形似、业务会分叉** 的代码 → 不强制抽；硬抽比重复更糟。
- 抽取后调用方应更短、意图更清晰；不要为「抽象完整」引入过深间接层。
- 优先复用项目内已有工具；禁止复制粘贴改几个字段变成第三份。

```ts
// ❌ 三个 Dialog 各写一遍 history:add 拼装
// ✅ 抽 saveHistory({ moduleName, appName, content, status })
// ❌ 两个仅 UI 文案不同的流程硬抽成巨型配置引擎
```

## 性能（先分场景）

本应用默认是 **本机单用户桌面端**，不是多租户服务端。

| 场景 | 原则 |
|------|------|
| 单用户本地 | 优先交互流畅：少阻塞主进程、列表分页/按需加载、避免无意义全量重渲染；不必上分布式锁、连接池、复杂缓存层 |
| 多用户 / 共享后端 | 才考虑限流、队列、幂等、并发写冲突、批量 IO；未引入共享服务前不要按服务端规模预支复杂度 |

落地检查：

- 主进程：重 CPU / 大文件 / 长请求勿堵 UI；能异步则异步；SQLite 查询带分页，禁止默认一次捞全表。
- 渲染进程：大列表用分页或虚拟滚动；弹窗关闭释放定时器/监听；少用无依赖的深层 watch。
- IPC：合并可合并的往返，避免循环里 `invoke` 打爆。
- 网络：超时与失败可感知；不要无界重试。
