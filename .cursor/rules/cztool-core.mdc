---
description: CZTool 项目核心约定 — 架构、探码、改动边界
alwaysApply: true
---

# CZTool Core

桌面工具箱：Tauri 2 + Vue 3 + Vite + TypeScript。UI 以 Element Plus 为主。

## 探码

- 优先用 MCP `codegraph_explore`（本仓库工具名，不是旧的 code-review-graph）。
- 索引目录：`.codegraph/`。缺失时再 `codegraph init`；不要对 `$HOME` 或盘符根目录 init。
- 图覆盖不到的配置/文档，再退回 Read / Grep。

## 架构边界

- **前端（WebView）**：界面与交互；经 `src/api/desktop.ts` 调 Rust 命令，禁止直接碰 Tauri/Electron 全局。
- **后端（Rust）** `src-tauri/src/`：窗控、SQLite 历史、解锁、规则扫描、HTTP、自动更新。
- 新能力先判断放前端还是 Rust；需要文件系统 / DB / 受限网络 / 系统托盘 → Rust。

## 改动纪律

- 只改任务相关文件；不顺手重构、不改无关格式。
- 编码质量见同目录 `coding-quality.mdc`（DRY 边界 + 单/多用户性能）。
- 不擅自 commit / push；用户明确要求再做。
- 不改 README 模板文案，除非任务就是补文档。
- 第三方接口 URL / 解析逻辑 fragile — 未确认前不要「优化」或替换供应商。
- 回复用简体中文。
