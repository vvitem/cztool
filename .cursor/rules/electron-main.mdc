---
description: Electron 主进程与 preload 约定
globs: electron/**/*.{ts,js}
alwaysApply: false
---

# Electron Main / Preload

## 主进程

- 当前逻辑集中在 `electron/main/index.ts`；新增模块优先拆到同目录子文件（如 `history.ts`、`system.ts`），再在入口注册，避免继续堆单体。
- SQLite：`userData/data.db`，历史表字段保持兼容；改 schema 要同步读写与渲染层类型。
- 列表查询必须分页；写入走短事务，避免在 UI 热路径上做重计算。
- 系统能力（通知、关机、开机启动）走现有 IPC 命名：`system:*`、`settings:*`。
- macOS 关机等平台差异要显式处理，不要假设 Windows 命令可用。
- 默认可按单用户本机进程假设；仅当引入多窗口争用同一 DB 写路径时，再补串行化/锁，不要提前上服务端并发模型。

## Preload

- 新增 IPC 必须同时改：`preload` 白名单 + `main` handler + 渲染侧调用。
- `invoke` / `send` / `on` 渠道分开维护；无效 channel 应拒绝，不要静默放行。

```ts
// ❌ 直接暴露 ipcRenderer
// ✅ 仅白名单 channel 转发
```
